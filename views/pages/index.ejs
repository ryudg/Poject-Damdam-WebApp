<!DOCTYPE html>
<html lang="ko">
  <head>
    <meta charset="UTF-8" />
    <meta http-equiv="X-UA-Compatible" content="IE=edge" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>Document</title>
    <!-- css -->
    <link rel="stylesheet" href="/css/splash.css" />
  </head>
  <body id="splash">
    <div class="splash">담담</div>

    <script>
      setTimeout(() => {
        fetch("/json/userData.json")
          .then((res) => res.json())
          .then((data) => {
            console.log(data[0].이름);
            let link = "http://localhost:3001/main";
            let link2 = "http://localhost:3001/username";
            let link3 = "http://localhost:3001/noinfomain";

            if (data[0].이름.length > 0 && data[0].흡연시작.length > 0) {
              location.href = link;
            } else if (data[0].이름.length) {
              location.href = link3;
            } else {
              location.href = link2;
            }

            //
          });
      }, 2000);
    </script>
  </body>
</html>
